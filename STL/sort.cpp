#include<bits/stdc++.h>
using namespace std;

/* 
    It can be used to sort an array or a vector or string.
    syntax
    int a[n]        vector<int> v;              string s;
    sort(a,a+n)     sort(v.begin(),v.end())     sort(s.begin(),s.end())
*/

int main(){

}